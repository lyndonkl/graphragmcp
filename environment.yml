name: graphragmcp
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
    - mcp>=1.0.0
    - typing-extensions>=4.7.0
    # Optional testing dependencies
    # - openai>=1.0.0
    # - anthropic>=0.7.0
    # - pytest>=7.0.0
    # - pytest-asyncio>=0.21.0
    # - black>=23.0.0
    # - mypy>=1.5.0